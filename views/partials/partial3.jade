extends ../index

block signInModal
	.modal.hide.fade(ng-controller="SignInCtrl", id="signIn")
 	.modal-header
    	button(type="button", class="close", data-dismiss="modal", aria-hidden="true") &times;
    	h3 Sign In
  	.modal-body
    	p One fine body…
  	.modal-footer
    	a(href="#", class="btn") Close
    	a(href="#", class="btn btn-primary") Save changes