// Generated by CoffeeScript 1.6.2
  'use strict';
  var AppCtrl, Controllers, TimeLineCtrl, UploadCtrl;

  AppCtrl = (function() {
    function AppCtrl($scope, $http) {
      this.$scope = $scope;
      this.$http = $http;
      this.getName();
      this.getAssignments();
      this.getSelectedAssignment();
      this.getProblems();
    }

    AppCtrl.prototype.getName = function() {
      var _this = this;

      return this.$http({
        method: 'GET',
        url: '/api/name'
      }).success(function(data, status, headers, config) {
        return _this.$scope.name = data.name;
      }).error(function(data, status, headers, config) {
        return _this.$scope.name = 'Error!';
      });
    };

    AppCtrl.prototype.getAssignments = function() {
      var _this = this;

      return this.$http({
        method: 'GET',
        url: 'api/assignments'
      }).success(function(data, status, headers, config) {
        _this.$scope.assignments = data;
        return console.log("Assignments retrieved.");
      }).error(function(data, status, headers, config) {
        _this.$scope.assignments = [];
        return console.log("Error retrieving assignments");
      });
    };

    AppCtrl.prototype.getSelectedAssignment = function() {
      return this.$scope.selectedAssignment = this.$scope.assignments[0].id;
    };

    AppCtrl.prototype.getProblems = function() {
      return this.$scope.problems = this.$scope.assignments[0].problems;
    };

    AppCtrl.$inject = ['$scope', '$http'];

    return AppCtrl;

  })();

  UploadCtrl = (function() {
    function UploadCtrl() {}

    UploadCtrl.$inject = ['$scope', '$http'];

    return UploadCtrl;

  })();

  TimeLineCtrl = (function() {
    function TimeLineCtrl() {}

    TimeLineCtrl.$inject = [];

    return TimeLineCtrl;

  })();

  Controllers = {
    "AppCtrl": AppCtrl,
    "UploadCtrl": UploadCtrl,
    "TimeLineCtrl": TimeLineCtrl
  };

