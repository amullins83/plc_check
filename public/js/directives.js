// Generated by CoffeeScript 1.6.1
'use strict';
angular.module('plcGrader.directives', []).directive('appVersion', [
  'version', function(version) {
    return function(scope, elm, attrs) {
      return elm.text(version);
    };
  }
]).directive('validColor', [
  'minWords', 'maxWords', function(minWords, maxWords) {
    return function(scope, elm, attrs) {
      var setColor;
      setColor = function() {
        var numWords, textColor;
        numWords = elm.text();
        if (numWords <= maxWords && numWords >= minWords) {
          textColor = "#00aa00";
        } else {
          textColor = "#ff8888";
        }
        return elm.css({
          color: textColor
        });
      };
      $(document).ready(setColor);
      return $(document).on("keyup", "textarea", setColor);
    };
  }
]);
